name: Deploy Next.js site to IONOS

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Build with Next.js
        run: npm run build
      
      - name: Static HTML export with Next.js
        run: npm run export
      
      - name: Upload artifact for deployment
        uses: actions/upload-artifact@v2
        with:
          name: nextjs-site
          path: ./out

  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Download artifact
        uses: actions/download-artifact@v2
        with:
          name: nextjs-site
    
      - name: Deploy to IONOS via SFTP using SCP
        uses: appleboy/scp-action@master
        with:
          host: access794949688.webspace-data.io
          username: u98282221
          port: 22
          source: "./out/*"
          target: "/out"
          password: IUbf29guufU98&/  # Passwort direkt angegeben (Nicht empfohlen)
